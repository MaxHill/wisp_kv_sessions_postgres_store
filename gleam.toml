name = "wisp_kv_sessions_postgres_store"
version = "0.0.2"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
#
description = "Postgres driver for wisp_kv_sessions"
licences = ["Apache-2.0"]
repository = { type = "github", user = "MaxHill", repo = "wisp_kv_sessions_postgres_store" }
# links = [{ title = "Website", href = "" }]
#
# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
gleam_stdlib = ">= 0.34.0 and < 2.0.0"
gleam_pgo = ">= 0.14.0 and < 1.0.0"
# wisp_kv_sessions = {path= "../wisp_kv_sessions/"}
birl = ">= 1.7.1 and < 2.0.0"
gleam_json = ">= 1.0.1 and < 2.0.0"
wisp = ">= 1.1.0 and < 2.0.0"
wisp_kv_sessions = ">= 0.0.2 and < 1.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
